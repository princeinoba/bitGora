{
  "name": "bitgora-market-lab",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A privacy-first, non-custodial Bitcoin-priced marketplace portfolio demonstration.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "verify": "npm run lint && npm test && npm run build && node scripts/verify-build.mjs && npm run smoke && node scripts/verify-determinism.mjs",
    "check": "npm run verify"
  }
}
